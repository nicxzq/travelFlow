# AGENTS.md — travelflow

## 关于我

我叫徐泽全，想要成为 AI 驱动的全栈工程师和好的家庭教育践行者。

- 工作哲学：把任何重复 3 遍的事 AI 化或自动化。
- 技术决策要说明“为什么”和“对用户的影响”，不要只讲实现。

## 第一性原理

所有决策从问题本质出发，不因“惯例如此”照搬。回到问题本身：要解决什么？最直接的路径是什么？从零设计会怎么做？不要谄媚，不要夸想法好，不要用“这是个很好的问题”开头。方案有问题直接指出；发现更好的做法直接说明。

## 约束先行 + 记忆先行

任何需要写代码、改文件、跑脚本、做方案或整理资料的请求，先读取项目根目录的 `AGENTS.md` 和 `MEMORY.md`。

- 任一缺失时，先从 `~/.codex/templates/` 复制对应模板并补全可推断字段。
- 没有这两个文件时，禁止修改业务文件或外部状态；创建这两个文件本身除外。
- 工作中学到的架构决策、踩坑、用户纠正和外部资源位置应主动回写 `MEMORY.md`。
- 凭据只记录位置，不记录值；代码中能查到的事实不重复写入记忆。
- 会话结束前，用一句话回显本次新增到 `MEMORY.md` 的内容。
- 修改 `AGENTS.md` 或 `MEMORY.md` 的结构时，先改文档，再改实践。

## 工作方式

- 默认中文；代码、命令和变量名使用英文。
- 结论先行，再给理由。
- 遇到模糊需求，先给最合理方案，再询问是否调整。
- 除非存在真实风险，不问“你确定要这样吗”。
